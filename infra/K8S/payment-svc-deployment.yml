apiVersion: apps/v1
kind: Deployment
metadata:
  name: sproutvrprovider-service-payments
  labels:
    app: sproutvrprovider-service-payments
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sproutvrprovider-service-payments
  template:
    metadata:
      labels:
        app: sproutvrprovider-service-payments
    spec:
      containers:
        - name: payments-service
          image: anonyfriday/sproutvrprovider-service-payments:prod
          imagePullPolicy: Always
          ports:
            - name: web
              containerPort: 80
            - name: grpc
              containerPort: 7707
          envFrom:
            - configMapRef:
                name: sproutvrprovider-service-payments-config
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            allowPrivilegeEscalation: true
---
apiVersion: v1
kind: Service
metadata:
  name: sproutvrprovider-service-payments-clusterip
spec:
  type: ClusterIP
  selector:
    app: sproutvrprovider-service-payments
  ports:
    - name: web
      port: 80
      targetPort: 80
    - name: grpc
      port: 7707
      targetPort: 7707
