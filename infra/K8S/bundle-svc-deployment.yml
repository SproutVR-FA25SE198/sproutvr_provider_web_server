apiVersion: apps/v1
kind: Deployment
metadata:
  name: sproutvrprovider-service-bundles
  labels:
    app: sproutvrprovider-service-bundles
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sproutvrprovider-service-bundles
  template:
    metadata:
      labels:
        app: sproutvrprovider-service-bundles
    spec:
      containers:
        - name: bundles-service
          image: anonyfriday/sproutvrprovider-service-bundles:prod
          imagePullPolicy: Always
          ports:
            - name: web
              containerPort: 80
            - name: grpc
              containerPort: 7707
          envFrom:
            - configMapRef:
                name: sproutvrprovider-service-bundles-config
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            allowPrivilegeEscalation: true
---
apiVersion: v1
kind: Service
metadata:
  name: sproutvrprovider-service-bundles-clusterip
spec:
  type: ClusterIP
  selector:
    app: sproutvrprovider-service-bundles
  ports:
    - name: web
      port: 80
      targetPort: 80
    - name: grpc
      port: 7707
      targetPort: 7707
