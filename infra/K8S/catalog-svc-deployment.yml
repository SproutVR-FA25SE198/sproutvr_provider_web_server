apiVersion: apps/v1
kind: Deployment
metadata:
  name: sproutvrprovider-service-catalogs
  labels:
    app: sproutvrprovider-service-catalogs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sproutvrprovider-service-catalogs
  template:
    metadata:
      labels:
        app: sproutvrprovider-service-catalogs
    spec:
      containers:
        - name: catalogs-service
          image: anonyfriday/sproutvrprovider-service-catalogs:prod
          imagePullPolicy: Always
          ports:
            - name: web
              containerPort: 80
            - name: grpc
              containerPort: 7707
          envFrom:
            - configMapRef:
                name: sproutvrprovider-service-catalogs-config
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            allowPrivilegeEscalation: true
---
apiVersion: v1
kind: Service
metadata:
  name: sproutvrprovider-service-catalogs-clusterip
spec:
  type: ClusterIP
  selector:
    app: sproutvrprovider-service-catalogs
  ports:
    - name: web
      port: 80
      targetPort: 80
    - name: grpc
      port: 7707
      targetPort: 7707
